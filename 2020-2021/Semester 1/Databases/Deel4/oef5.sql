USE `gamemania`;

/*
-- 1
SELECT * FROM `tblconsoles`;

-- 2
SELECT
	REPLACE(`naam`, " ", "_") AS "Uitgever"
FROM
	`tbluitgevers`;
    
-- 3
SELECT
	LCASE(`naam`)
FROM
	`tblgamedefinitions`;
    
-- 4
SELECT
	REPLACE(LCASE(`naam`), " ", "_") AS "Game"
FROM
	`tblgamedefinitions`;
    
-- 5
SELECT
	CONCAT(MONTH(`releasedatum`), "-", DAY(`releasedatum`), "-", YEAR(`releasedatum`)) AS "Realeasedatum"
FROM
	`tblconsoles`;

-- 6
SELECT
	`naam`,
    LENGTH(`naam`) AS "Length"
FROM
	`tblgamedefinitions

-- 7
SELECT
	MAX(`price`) AS "Hoogste_prijs"
FROM
	`tblgames`;

-- 8
SELECT
	SUM(`price`) AS "max_omzet"
FROM
	`tblgames`;

-- 9
SELECT
	`gamedefinitionid`,
    `consoleid`,
    `price`,
    `btw_percentage`,
    CASE (`gametypeid`)
		WHEN 1 THEN "Nieuw"
        WHEN 2 THEN "Occassie"
	END AS "gametype"
FROM 
	`tblgames`;
*/

-- 10
-- HOE TF DOE JE DIT
	
